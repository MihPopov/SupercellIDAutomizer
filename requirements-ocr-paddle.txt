# Optional OCR backend. Install in addition to requirements.txt to use OCR_ENGINE=paddle.
paddleocr>=3.0.0
# Install the appropriate PaddlePaddle runtime for your CPU/GPU from the official PaddlePaddle selector.
